[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "MobileDogs"
authors = [{name = "Demid Antipin", email = "antipindemid@inbox.ru"}]
dynamic = ["version", "description"]
